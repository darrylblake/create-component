import React from "react";
import { render } from "@testing-library/react";
import { {{componentName}} } from "./{{componentName}}";

it("should render correctly", () => {
  const { container } = render(<{{componentName}}>Children</{{componentName}}>);
});
